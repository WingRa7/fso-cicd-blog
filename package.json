{
  "name": "fso-cicd-blog",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Tom Raine",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "build:local": "npm run install:client && npm run build:client",
    "lint": "eslint .",
    "install:client": "npm install --prefix client --include=dev",
    "build:client": "npm run build --prefix client",
    "lint:client": "npm run lint --prefix client",
    "test:client": "npm run test --prefix client",
    "clean:client": "rm -rf client/node_modules",
    "test": "NODE_ENV=test node --test",
    "start:test": "NODE_ENV=test node index.js",
    "build": "npm install && npm run install:client && npm run build:client && npm run clean:client"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.14.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "@flydotio/dockerfile": "^0.7.10",
    "eslint": "^9.24.0",
    "globals": "^16.0.0",
    "supertest": "^7.1.0"
  }
}
